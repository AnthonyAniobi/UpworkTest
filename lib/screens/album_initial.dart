import 'package:flutter/material.dart';

class AlbumInitialScreen extends StatelessWidget {
  const AlbumInitialScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
